# CONFIGURAÇÕES GERAIS
spring.application.name=desafio-backend
server.port=8080

# CONEXÃO COM O POSTGRESQL (Via .env)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

# HIBERNATE & FLYWAY

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# DESLIGANDO SECURITY TEMPORARIAMENTE
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# CONFIGURAÇÕES DO JWT
api.security.token.expiration=86400000
api.security.token.secret=U2VuaGFTdXBlclNlY3JldGFEaWZpY2lsRGVEZXNjb2JyaXJEZXNhZmlvU2Vh

